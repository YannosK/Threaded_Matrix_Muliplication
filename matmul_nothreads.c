#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define size 4

void matrix_init(int *A, int dim);

int main(void)
{
    int A[size][size];
    int B[size][size];
    int C[size][size];

    return 0;
}

void matrix_init(int *A, int dim)
{
}